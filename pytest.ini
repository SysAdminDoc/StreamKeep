[pytest]
testpaths = tests
python_files = test_*.py
python_classes = *Tests Test*
python_functions = test_*
addopts = --cov=streamkeep --cov-report=term-missing:skip-covered
markers =
    slow: drives real ffmpeg in realtime; seconds per test, not milliseconds
