# Core runtime dependencies for source checkouts.
# Requires Python 3.11+ (3.10 EOL Oct 2026; yt-dlp minimum is 3.11).
PyQt6>=6.11.0
# Pin the Qt runtime floor independently of the PyQt6 wrapper version.
PyQt6-Qt6>=6.11.1
Pillow>=12.3.0
yt-dlp[default]>=2026.07.04
send2trash>=1.8
websocket-client>=1.7
keyring>=25.0
argon2-cffi>=25.1.0
cryptography>=50.0.0
defusedxml>=0.7.1
PyYAML>=6.0.2
urllib3>=2.7.0
paramiko>=5.0.0

# Optional runtime extras. These remain out of requirements.lock because
# libmpv is a platform-managed native library and S3 support is opt-in.
# packaging/sbom.py records each declaration in the release SBOM.
# python-mpv>=1.0.8
# libmpv>=0.41.0  # native runtime; required by python-mpv
# boto3>=1.43.0
